{% extends 'AcmeRememberSeriesBundle::base.html.twig' %}

{% block body %}
    <ul>
    {% for item in series %}
        <li><a href="{{ path('acme_remember_series_single_page', { 'series_id': item.id }) }}">{{ item.name }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}
